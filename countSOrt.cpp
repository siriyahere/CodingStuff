#include<stdio.h>

char* countSort(int *arr,int n)
{
	int i;
	bool cnt[n];
	char *str;
	memset(str,'\0',n*10);
	memset(cnt,false,n);
	for(i=0;i<n;i++)
	{
		cnt[a[i]]=true;
	}
	for(i=0;i<n;i++)
	{
		if(!cnt[a[i]]){
		
			strcat(str,a[i]);
			strcat(str," ");
		}
	}
	return str;
}
int main()
{
	int tc;
	scanf("%d",&tc);
	while(tc--)
	{
		char s
		cin>>s;
	}
	
}
